# Comma-separated list of origins the API will accept requests from.
# In development this points to your local Next.js server.
# In production replace with your Vercel (or equivalent) URL.
CORS_ORIGINS=http://localhost:3000
